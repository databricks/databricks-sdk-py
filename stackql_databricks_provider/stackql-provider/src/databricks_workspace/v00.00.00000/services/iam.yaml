openapi: 3.0.0
info:
  title: Databricks Iam API (workspace)
  description: >-
    OpenAPI specification for the Databricks iam service (workspace-level APIs),
    generated from the Databricks Python SDK.
  version: 0.1.0
  x-stackql-sdk-version: 0.86.0
  x-stackql-date-generated: '2026-02-12'
  x-stackql-sdk-namespace: databricks.sdk.service.iam
servers:
  - url: https://{deployment_name}.cloud.databricks.com
    variables:
      deployment_name:
        description: The Databricks Workspace Deployment Name
        default: dbc-abcd0123-a1bc
paths:
  /api/2.0/access-control/check-policy-v2:
    get:
      operationId: access_control_check_policy
      summary: Check access policy to a resource.
      tags:
        - iam
        - access_control
      description: |-
        Check access policy to a resource.

        :param actor: :class:`Actor`
        :param permission: str
        :param resource: str
          Ex: (servicePrincipal/use, accounts/<account-id>/servicePrincipals/<sp-id>) Ex:
          (servicePrincipal.ruleSet/update, accounts/<account-id>/servicePrincipals/<sp-id>/ruleSets/default)
        :param consistency_token: :class:`ConsistencyToken`
        :param authz_identity: :class:`RequestAuthzIdentity`
        :param resource_info: :class:`ResourceInfo` (optional)

        :returns: :class:`CheckPolicyResponse`
      parameters:
        - name: actor
          in: query
          required: true
          schema:
            type: string
          description: ':param permission: str'
        - name: permission
          in: query
          required: true
          schema:
            type: string
        - name: resource
          in: query
          required: true
          schema:
            type: string
          description: >-
            Ex: (servicePrincipal/use,
            accounts/<account-id>/servicePrincipals/<sp-id>) Ex:
            (servicePrincipal.ruleSet/update,
            accounts/<account-id>/servicePrincipals/<sp-id>/ruleSets/default)
        - name: consistency_token
          in: query
          required: true
          schema:
            type: string
          description: ':param authz_identity: :class:`RequestAuthzIdentity`'
        - name: authz_identity
          in: query
          required: true
          schema:
            type: string
        - name: resource_info
          in: query
          required: false
          schema:
            type: string
          description: ':returns: :class:`CheckPolicyResponse`'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CheckPolicyResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: AccessControlAPI
  /api/2.0/preview/accounts/access-control/assignable-roles:
    get:
      operationId: account_access_control_proxy_get_assignable_roles_for_resource
      summary: >-
        Gets all the roles that can be granted on an account level resource. A
        role is grantable if the rule
      tags:
        - iam
        - account_access_control_proxy
      description: >-
        Gets all the roles that can be granted on an account level resource. A
        role is grantable if the rule

        set on the resource can contain an access rule of the role.


        :param resource: str
          The resource name for which assignable roles will be listed.

          Examples | Summary :--- | :--- `resource=accounts/<ACCOUNT_ID>` | A resource name for the account.
          `resource=accounts/<ACCOUNT_ID>/groups/<GROUP_ID>` | A resource name for the group.
          `resource=accounts/<ACCOUNT_ID>/servicePrincipals/<SP_ID>` | A resource name for the service
          principal. `resource=accounts/<ACCOUNT_ID>/tagPolicies/<TAG_POLICY_ID>` | A resource name for the
          tag policy.

        :returns: :class:`GetAssignableRolesForResourceResponse`
      parameters:
        - name: resource
          in: query
          required: true
          schema:
            type: string
          description: >-
            The resource name for which assignable roles will be listed.
            Examples | Summary :--- | :--- `resource=accounts/<ACCOUNT_ID>` | A
            resource name for the account.
            `resource=accounts/<ACCOUNT_ID>/groups/<GROUP_ID>` | A resource name
            for the group.
            `resource=accounts/<ACCOUNT_ID>/servicePrincipals/<SP_ID>` | A
            resource name for the service principal.
            `resource=accounts/<ACCOUNT_ID>/tagPolicies/<TAG_POLICY_ID>` | A
            resource name for the tag policy.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetAssignableRolesForResourceResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: AccountAccessControlProxyAPI
  /api/2.0/preview/accounts/access-control/rule-sets:
    get:
      operationId: account_access_control_proxy_get_rule_set
      summary: >-
        Get a rule set by its name. A rule set is always attached to a resource
        and contains a list of access
      tags:
        - iam
        - account_access_control_proxy
      description: >-
        Get a rule set by its name. A rule set is always attached to a resource
        and contains a list of access

        rules on the said resource. Currently only a default rule set for each
        resource is supported.


        :param name: str
          The ruleset name associated with the request.

          Examples | Summary :--- | :--- `name=accounts/<ACCOUNT_ID>/ruleSets/default` | A name for a rule set
          on the account. `name=accounts/<ACCOUNT_ID>/groups/<GROUP_ID>/ruleSets/default` | A name for a rule
          set on the group.
          `name=accounts/<ACCOUNT_ID>/servicePrincipals/<SERVICE_PRINCIPAL_APPLICATION_ID>/ruleSets/default` |
          A name for a rule set on the service principal.
          `name=accounts/<ACCOUNT_ID>/tagPolicies/<TAG_POLICY_ID>/ruleSets/default` | A name for a rule set on
          the tag policy.
        :param etag: str
          Etag used for versioning. The response is at least as fresh as the eTag provided. Etag is used for
          optimistic concurrency control as a way to help prevent simultaneous updates of a rule set from
          overwriting each other. It is strongly suggested that systems make use of the etag in the read ->
          modify -> write pattern to perform rule set updates in order to avoid race conditions that is get an
          etag from a GET rule set request, and pass it with the PUT update request to identify the rule set
          version you are updating.

          Examples | Summary :--- | :--- `etag=` | An empty etag can only be used in GET to indicate no
          freshness requirements. `etag=RENUAAABhSweA4NvVmmUYdiU717H3Tgy0UJdor3gE4a+mq/oj9NjAf8ZsQ==` | An
          etag encoded a specific version of the rule set to get or to be updated.

        :returns: :class:`RuleSetResponse`
      parameters:
        - name: name
          in: query
          required: true
          schema:
            type: string
          description: >-
            The ruleset name associated with the request. Examples | Summary
            :--- | :--- `name=accounts/<ACCOUNT_ID>/ruleSets/default` | A name
            for a rule set on the account.
            `name=accounts/<ACCOUNT_ID>/groups/<GROUP_ID>/ruleSets/default` | A
            name for a rule set on the group.
            `name=accounts/<ACCOUNT_ID>/servicePrincipals/<SERVICE_PRINCIPAL_APPLICATION_ID>/ruleSets/default`
            | A name for a rule set on the service principal.
            `name=accounts/<ACCOUNT_ID>/tagPolicies/<TAG_POLICY_ID>/ruleSets/default`
            | A name for a rule set on the tag policy.
        - name: etag
          in: query
          required: true
          schema:
            type: string
          description: >-
            Etag used for versioning. The response is at least as fresh as the
            eTag provided. Etag is used for optimistic concurrency control as a
            way to help prevent simultaneous updates of a rule set from
            overwriting each other. It is strongly suggested that systems make
            use of the etag in the read -> modify -> write pattern to perform
            rule set updates in order to avoid race conditions that is get an
            etag from a GET rule set request, and pass it with the PUT update
            request to identify the rule set version you are updating. Examples
            | Summary :--- | :--- `etag=` | An empty etag can only be used in
            GET to indicate no freshness requirements.
            `etag=RENUAAABhSweA4NvVmmUYdiU717H3Tgy0UJdor3gE4a+mq/oj9NjAf8ZsQ==`
            | An etag encoded a specific version of the rule set to get or to be
            updated.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuleSetResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: AccountAccessControlProxyAPI
    put:
      operationId: account_access_control_proxy_update_rule_set
      summary: >-
        Replace the rules of a rule set. First, use get to read the current
        version of the rule set before
      tags:
        - iam
        - account_access_control_proxy
      description: >-
        Replace the rules of a rule set. First, use get to read the current
        version of the rule set before

        modifying it. This pattern helps prevent conflicts between concurrent
        updates.


        :param name: str
          Name of the rule set.
        :param rule_set: :class:`RuleSetUpdateRequest`


        :returns: :class:`RuleSetResponse`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Name of the rule set.
                rule_set:
                  type: string
                  description: ':returns: :class:`RuleSetResponse`'
              required:
                - name
                - rule_set
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuleSetResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: AccountAccessControlProxyAPI
  /api/2.0/preview/scim/v2/Me:
    get:
      operationId: current_user_me
      summary: Get details about the current method caller's identity.
      tags:
        - iam
        - current_user
      description: |-
        Get details about the current method caller's identity.


        :returns: :class:`User`
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: CurrentUserAPI
  /api/2.0/preview/scim/v2/Groups:
    post:
      operationId: groups_v2_create
      summary: >-
        Creates a group in the Databricks workspace with a unique name, using
        the supplied group details.
      tags:
        - iam
        - groups_v2
      description: >-
        Creates a group in the Databricks workspace with a unique name, using
        the supplied group details.


        :param display_name: str (optional)
          String that represents a human-readable group name
        :param entitlements: List[:class:`ComplexValue`] (optional)
          Entitlements assigned to the group. See [assigning entitlements] for a full list of supported
          values.

          [assigning entitlements]: https://docs.databricks.com/administration-guide/users-groups/index.html#assigning-entitlements
        :param external_id: str (optional)

        :param groups: List[:class:`ComplexValue`] (optional)

        :param id: str (optional)
          Databricks group ID
        :param members: List[:class:`ComplexValue`] (optional)

        :param meta: :class:`ResourceMeta` (optional)
          Container for the group identifier. Workspace local versus account.
        :param roles: List[:class:`ComplexValue`] (optional)
          Corresponds to AWS instance profile/arn role.
        :param schemas: List[:class:`GroupSchema`] (optional)
          The schema of the group.

        :returns: :class:`Group`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                display_name:
                  type: string
                  description: String that represents a human-readable group name
                entitlements:
                  type: string
                  description: >-
                    Entitlements assigned to the group. See [assigning
                    entitlements] for a full list of supported values.
                    [assigning entitlements]:
                    https://docs.databricks.com/administration-guide/users-groups/index.html#assigning-entitlements
                external_id:
                  type: string
                  description: ':param groups: List[:class:`ComplexValue`] (optional)'
                groups:
                  type: string
                id:
                  type: string
                  description: Databricks group ID
                members:
                  type: string
                  description: >-
                    :param meta: :class:`ResourceMeta` (optional) Container for
                    the group identifier. Workspace local versus account.
                meta:
                  type: string
                roles:
                  type: string
                  description: Corresponds to AWS instance profile/arn role.
                schemas:
                  type: string
                  description: The schema of the group.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Group'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: GroupsV2API
    get:
      operationId: groups_v2_list
      summary: Gets all details of the groups associated with the Databricks workspace.
      tags:
        - iam
        - groups_v2
      description: |-
        Gets all details of the groups associated with the Databricks workspace.

        :param attributes: str (optional)
          Comma-separated list of attributes to return in response.
        :param count: int (optional)
          Desired number of results per page.
        :param excluded_attributes: str (optional)
          Comma-separated list of attributes to exclude in response.
        :param filter: str (optional)
          Query by which the results have to be filtered. Supported operators are equals(`eq`),
          contains(`co`), starts with(`sw`) and not equals(`ne`). Additionally, simple expressions can be
          formed using logical operators - `and` and `or`. The [SCIM RFC] has more details but we currently
          only support simple expressions.

          [SCIM RFC]: https://tools.ietf.org/html/rfc7644#section-3.4.2.2
        :param sort_by: str (optional)
          Attribute to sort the results.
        :param sort_order: :class:`ListSortOrder` (optional)
          The order to sort the results.
        :param start_index: int (optional)
          Specifies the index of the first result. First item is number 1.

        :returns: Iterator over :class:`Group`
      parameters:
        - name: attributes
          in: query
          required: false
          schema:
            type: string
          description: Comma-separated list of attributes to return in response.
        - name: count
          in: query
          required: false
          schema:
            type: string
          description: Desired number of results per page.
        - name: excluded_attributes
          in: query
          required: false
          schema:
            type: string
          description: Comma-separated list of attributes to exclude in response.
        - name: filter
          in: query
          required: false
          schema:
            type: string
          description: >-
            Query by which the results have to be filtered. Supported operators
            are equals(`eq`), contains(`co`), starts with(`sw`) and not
            equals(`ne`). Additionally, simple expressions can be formed using
            logical operators - `and` and `or`. The [SCIM RFC] has more details
            but we currently only support simple expressions. [SCIM RFC]:
            https://tools.ietf.org/html/rfc7644#section-3.4.2.2
        - name: sort_by
          in: query
          required: false
          schema:
            type: string
          description: Attribute to sort the results.
        - name: sort_order
          in: query
          required: false
          schema:
            type: string
          description: The order to sort the results.
        - name: start_index
          in: query
          required: false
          schema:
            type: string
          description: Specifies the index of the first result. First item is number 1.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListGroupsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: GroupsV2API
  /api/2.0/preview/scim/v2/Groups/{id}:
    delete:
      operationId: groups_v2_delete
      summary: Deletes a group from the Databricks workspace.
      tags:
        - iam
        - groups_v2
      description: |-
        Deletes a group from the Databricks workspace.

        :param id: str
          Unique ID for a group in the Databricks workspace.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Unique ID for a group in the Databricks workspace.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: GroupsV2API
    get:
      operationId: groups_v2_get
      summary: Gets the information for a specific group in the Databricks workspace.
      tags:
        - iam
        - groups_v2
      description: |-
        Gets the information for a specific group in the Databricks workspace.

        :param id: str
          Unique ID for a group in the Databricks workspace.

        :returns: :class:`Group`
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Unique ID for a group in the Databricks workspace.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Group'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: GroupsV2API
    patch:
      operationId: groups_v2_patch
      summary: Partially updates the details of a group.
      tags:
        - iam
        - groups_v2
      description: |-
        Partially updates the details of a group.

        :param id: str
          Unique ID in the Databricks workspace.
        :param operations: List[:class:`Patch`] (optional)
        :param schemas: List[:class:`PatchSchema`] (optional)
          The schema of the patch request. Must be ["urn:ietf:params:scim:api:messages:2.0:PatchOp"].
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Unique ID in the Databricks workspace.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                operations:
                  type: string
                  description: >-
                    :param schemas: List[:class:`PatchSchema`] (optional) The
                    schema of the patch request. Must be
                    ["urn:ietf:params:scim:api:messages:2.0:PatchOp"].
                schemas:
                  type: string
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: GroupsV2API
    put:
      operationId: groups_v2_update
      summary: Updates the details of a group by replacing the entire group entity.
      tags:
        - iam
        - groups_v2
      description: |-
        Updates the details of a group by replacing the entire group entity.

        :param id: str
          Databricks group ID
        :param display_name: str (optional)
          String that represents a human-readable group name
        :param entitlements: List[:class:`ComplexValue`] (optional)
          Entitlements assigned to the group. See [assigning entitlements] for a full list of supported
          values.

          [assigning entitlements]: https://docs.databricks.com/administration-guide/users-groups/index.html#assigning-entitlements
        :param external_id: str (optional)
        :param groups: List[:class:`ComplexValue`] (optional)
        :param members: List[:class:`ComplexValue`] (optional)
        :param meta: :class:`ResourceMeta` (optional)
          Container for the group identifier. Workspace local versus account.
        :param roles: List[:class:`ComplexValue`] (optional)
          Corresponds to AWS instance profile/arn role.
        :param schemas: List[:class:`GroupSchema`] (optional)
          The schema of the group.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Databricks group ID
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                display_name:
                  type: string
                  description: String that represents a human-readable group name
                entitlements:
                  type: string
                  description: >-
                    Entitlements assigned to the group. See [assigning
                    entitlements] for a full list of supported values.
                    [assigning entitlements]:
                    https://docs.databricks.com/administration-guide/users-groups/index.html#assigning-entitlements
                external_id:
                  type: string
                  description: ':param groups: List[:class:`ComplexValue`] (optional)'
                groups:
                  type: string
                members:
                  type: string
                  description: >-
                    :param meta: :class:`ResourceMeta` (optional) Container for
                    the group identifier. Workspace local versus account.
                meta:
                  type: string
                roles:
                  type: string
                  description: Corresponds to AWS instance profile/arn role.
                schemas:
                  type: string
                  description: The schema of the group.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: GroupsV2API
  /api/2.0/permissionmigration:
    post:
      operationId: permission_migration_migrate_permissions
      summary: Migrate Permissions.
      tags:
        - iam
        - permission_migration
      description: |-
        Migrate Permissions.

        :param workspace_id: int
          WorkspaceId of the associated workspace where the permission migration will occur.
        :param from_workspace_group_name: str
          The name of the workspace group that permissions will be migrated from.
        :param to_account_group_name: str
          The name of the account group that permissions will be migrated to.
        :param size: int (optional)
          The maximum number of permissions that will be migrated.

        :returns: :class:`MigratePermissionsResponse`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                workspace_id:
                  type: integer
                  description: >-
                    WorkspaceId of the associated workspace where the permission
                    migration will occur.
                from_workspace_group_name:
                  type: string
                  description: >-
                    The name of the workspace group that permissions will be
                    migrated from.
                to_account_group_name:
                  type: string
                  description: >-
                    The name of the account group that permissions will be
                    migrated to.
                size:
                  type: string
                  description: The maximum number of permissions that will be migrated.
              required:
                - workspace_id
                - from_workspace_group_name
                - to_account_group_name
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MigratePermissionsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: PermissionMigrationAPI
  /api/2.0/permissions/{request_object_type}/{request_object_id}:
    get:
      operationId: permissions_get
      summary: >-
        Gets the permissions of an object. Objects can inherit permissions from
        their parent objects or root
      tags:
        - iam
        - permissions
      description: >-
        Gets the permissions of an object. Objects can inherit permissions from
        their parent objects or root

        object.


        :param request_object_type: str
          The type of the request object. Can be one of the following: alerts, alertsv2, authorization,
          clusters, cluster-policies, dashboards, dbsql-dashboards, directories, experiments, files, genie,
          instance-pools, jobs, notebooks, pipelines, queries, registered-models, repos, serving-endpoints, or
          warehouses.
        :param request_object_id: str
          The id of the request object.

        :returns: :class:`ObjectPermissions`
      parameters:
        - name: request_object_type
          in: path
          required: true
          schema:
            type: string
          description: >-
            The type of the request object. Can be one of the following: alerts,
            alertsv2, authorization, clusters, cluster-policies, dashboards,
            dbsql-dashboards, directories, experiments, files, genie,
            instance-pools, jobs, notebooks, pipelines, queries,
            registered-models, repos, serving-endpoints, or warehouses.
        - name: request_object_id
          in: path
          required: true
          schema:
            type: string
          description: The id of the request object.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ObjectPermissions'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: PermissionsAPI
    put:
      operationId: permissions_set
      summary: >-
        Sets permissions on an object, replacing existing permissions if they
        exist. Deletes all direct
      tags:
        - iam
        - permissions
      description: >-
        Sets permissions on an object, replacing existing permissions if they
        exist. Deletes all direct

        permissions if none are specified. Objects can inherit permissions from
        their parent objects or root

        object.


        :param request_object_type: str
          The type of the request object. Can be one of the following: alerts, alertsv2, authorization,
          clusters, cluster-policies, dashboards, dbsql-dashboards, directories, experiments, files, genie,
          instance-pools, jobs, notebooks, pipelines, queries, registered-models, repos, serving-endpoints, or
          warehouses.
        :param request_object_id: str
          The id of the request object.
        :param access_control_list: List[:class:`AccessControlRequest`]
        (optional)


        :returns: :class:`ObjectPermissions`
      parameters:
        - name: request_object_type
          in: path
          required: true
          schema:
            type: string
          description: >-
            The type of the request object. Can be one of the following: alerts,
            alertsv2, authorization, clusters, cluster-policies, dashboards,
            dbsql-dashboards, directories, experiments, files, genie,
            instance-pools, jobs, notebooks, pipelines, queries,
            registered-models, repos, serving-endpoints, or warehouses.
        - name: request_object_id
          in: path
          required: true
          schema:
            type: string
          description: The id of the request object.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                access_control_list:
                  type: string
                  description: ':returns: :class:`ObjectPermissions`'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ObjectPermissions'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: PermissionsAPI
    patch:
      operationId: permissions_update
      summary: >-
        Updates the permissions on an object. Objects can inherit permissions
        from their parent objects or
      tags:
        - iam
        - permissions
      description: >-
        Updates the permissions on an object. Objects can inherit permissions
        from their parent objects or

        root object.


        :param request_object_type: str
          The type of the request object. Can be one of the following: alerts, alertsv2, authorization,
          clusters, cluster-policies, dashboards, dbsql-dashboards, directories, experiments, files, genie,
          instance-pools, jobs, notebooks, pipelines, queries, registered-models, repos, serving-endpoints, or
          warehouses.
        :param request_object_id: str
          The id of the request object.
        :param access_control_list: List[:class:`AccessControlRequest`]
        (optional)


        :returns: :class:`ObjectPermissions`
      parameters:
        - name: request_object_type
          in: path
          required: true
          schema:
            type: string
          description: >-
            The type of the request object. Can be one of the following: alerts,
            alertsv2, authorization, clusters, cluster-policies, dashboards,
            dbsql-dashboards, directories, experiments, files, genie,
            instance-pools, jobs, notebooks, pipelines, queries,
            registered-models, repos, serving-endpoints, or warehouses.
        - name: request_object_id
          in: path
          required: true
          schema:
            type: string
          description: The id of the request object.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                access_control_list:
                  type: string
                  description: ':returns: :class:`ObjectPermissions`'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ObjectPermissions'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: PermissionsAPI
  /api/2.0/permissions/{request_object_type}/{request_object_id}/permissionLevels:
    get:
      operationId: permissions_get_permission_levels
      summary: Gets the permission levels that a user can have on an object.
      tags:
        - iam
        - permissions
      description: |-
        Gets the permission levels that a user can have on an object.

        :param request_object_type: str
          The type of the request object. Can be one of the following: alerts, alertsv2, authorization,
          clusters, cluster-policies, dashboards, dbsql-dashboards, directories, experiments, files, genie,
          instance-pools, jobs, notebooks, pipelines, queries, registered-models, repos, serving-endpoints, or
          warehouses.
        :param request_object_id: str

        :returns: :class:`GetPermissionLevelsResponse`
      parameters:
        - name: request_object_type
          in: path
          required: true
          schema:
            type: string
          description: >-
            The type of the request object. Can be one of the following: alerts,
            alertsv2, authorization, clusters, cluster-policies, dashboards,
            dbsql-dashboards, directories, experiments, files, genie,
            instance-pools, jobs, notebooks, pipelines, queries,
            registered-models, repos, serving-endpoints, or warehouses.
        - name: request_object_id
          in: path
          required: true
          schema:
            type: string
          description: ':returns: :class:`GetPermissionLevelsResponse`'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetPermissionLevelsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: PermissionsAPI
  /api/2.0/preview/scim/v2/ServicePrincipals:
    post:
      operationId: service_principals_v2_create
      summary: Creates a new service principal in the Databricks workspace.
      tags:
        - iam
        - service_principals_v2
      description: |-
        Creates a new service principal in the Databricks workspace.

        :param active: bool (optional)
          If this user is active
        :param application_id: str (optional)
          UUID relating to the service principal
        :param display_name: str (optional)
          String that represents a concatenation of given and family names.
        :param entitlements: List[:class:`ComplexValue`] (optional)
          Entitlements assigned to the service principal. See [assigning entitlements] for a full list of
          supported values.

          [assigning entitlements]: https://docs.databricks.com/administration-guide/users-groups/index.html#assigning-entitlements
        :param external_id: str (optional)
        :param groups: List[:class:`ComplexValue`] (optional)
        :param id: str (optional)
          Databricks service principal ID.
        :param roles: List[:class:`ComplexValue`] (optional)
          Corresponds to AWS instance profile/arn role.
        :param schemas: List[:class:`ServicePrincipalSchema`] (optional)
          The schema of the List response.

        :returns: :class:`ServicePrincipal`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                active:
                  type: string
                  description: If this user is active
                application_id:
                  type: string
                  description: UUID relating to the service principal
                display_name:
                  type: string
                  description: >-
                    String that represents a concatenation of given and family
                    names.
                entitlements:
                  type: string
                  description: >-
                    Entitlements assigned to the service principal. See
                    [assigning entitlements] for a full list of supported
                    values. [assigning entitlements]:
                    https://docs.databricks.com/administration-guide/users-groups/index.html#assigning-entitlements
                external_id:
                  type: string
                  description: ':param groups: List[:class:`ComplexValue`] (optional)'
                groups:
                  type: string
                id:
                  type: string
                  description: Databricks service principal ID.
                roles:
                  type: string
                  description: Corresponds to AWS instance profile/arn role.
                schemas:
                  type: string
                  description: The schema of the List response.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServicePrincipal'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ServicePrincipalsV2API
    get:
      operationId: service_principals_v2_list
      summary: >-
        Gets the set of service principals associated with a Databricks
        workspace.
      tags:
        - iam
        - service_principals_v2
      description: >-
        Gets the set of service principals associated with a Databricks
        workspace.


        :param attributes: str (optional)
          Comma-separated list of attributes to return in response.
        :param count: int (optional)
          Desired number of results per page.
        :param excluded_attributes: str (optional)
          Comma-separated list of attributes to exclude in response.
        :param filter: str (optional)
          Query by which the results have to be filtered. Supported operators are equals(`eq`),
          contains(`co`), starts with(`sw`) and not equals(`ne`). Additionally, simple expressions can be
          formed using logical operators - `and` and `or`. The [SCIM RFC] has more details but we currently
          only support simple expressions.

          [SCIM RFC]: https://tools.ietf.org/html/rfc7644#section-3.4.2.2
        :param sort_by: str (optional)
          Attribute to sort the results.
        :param sort_order: :class:`ListSortOrder` (optional)
          The order to sort the results.
        :param start_index: int (optional)
          Specifies the index of the first result. First item is number 1.

        :returns: Iterator over :class:`ServicePrincipal`
      parameters:
        - name: attributes
          in: query
          required: false
          schema:
            type: string
          description: Comma-separated list of attributes to return in response.
        - name: count
          in: query
          required: false
          schema:
            type: string
          description: Desired number of results per page.
        - name: excluded_attributes
          in: query
          required: false
          schema:
            type: string
          description: Comma-separated list of attributes to exclude in response.
        - name: filter
          in: query
          required: false
          schema:
            type: string
          description: >-
            Query by which the results have to be filtered. Supported operators
            are equals(`eq`), contains(`co`), starts with(`sw`) and not
            equals(`ne`). Additionally, simple expressions can be formed using
            logical operators - `and` and `or`. The [SCIM RFC] has more details
            but we currently only support simple expressions. [SCIM RFC]:
            https://tools.ietf.org/html/rfc7644#section-3.4.2.2
        - name: sort_by
          in: query
          required: false
          schema:
            type: string
          description: Attribute to sort the results.
        - name: sort_order
          in: query
          required: false
          schema:
            type: string
          description: The order to sort the results.
        - name: start_index
          in: query
          required: false
          schema:
            type: string
          description: Specifies the index of the first result. First item is number 1.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListServicePrincipalResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ServicePrincipalsV2API
  /api/2.0/preview/scim/v2/ServicePrincipals/{id}:
    delete:
      operationId: service_principals_v2_delete
      summary: Delete a single service principal in the Databricks workspace.
      tags:
        - iam
        - service_principals_v2
      description: |-
        Delete a single service principal in the Databricks workspace.

        :param id: str
          Unique ID for a service principal in the Databricks workspace.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Unique ID for a service principal in the Databricks workspace.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ServicePrincipalsV2API
    get:
      operationId: service_principals_v2_get
      summary: >-
        Gets the details for a single service principal define in the Databricks
        workspace.
      tags:
        - iam
        - service_principals_v2
      description: >-
        Gets the details for a single service principal define in the Databricks
        workspace.


        :param id: str
          Unique ID for a service principal in the Databricks workspace.

        :returns: :class:`ServicePrincipal`
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Unique ID for a service principal in the Databricks workspace.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServicePrincipal'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ServicePrincipalsV2API
    patch:
      operationId: service_principals_v2_patch
      summary: >-
        Partially updates the details of a single service principal in the
        Databricks workspace.
      tags:
        - iam
        - service_principals_v2
      description: >-
        Partially updates the details of a single service principal in the
        Databricks workspace.


        :param id: str
          Unique ID in the Databricks workspace.
        :param operations: List[:class:`Patch`] (optional)

        :param schemas: List[:class:`PatchSchema`] (optional)
          The schema of the patch request. Must be ["urn:ietf:params:scim:api:messages:2.0:PatchOp"].
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Unique ID in the Databricks workspace.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                operations:
                  type: string
                  description: >-
                    :param schemas: List[:class:`PatchSchema`] (optional) The
                    schema of the patch request. Must be
                    ["urn:ietf:params:scim:api:messages:2.0:PatchOp"].
                schemas:
                  type: string
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ServicePrincipalsV2API
    put:
      operationId: service_principals_v2_update
      summary: Updates the details of a single service principal.
      tags:
        - iam
        - service_principals_v2
      description: |-
        Updates the details of a single service principal.

        This action replaces the existing service principal with the same name.

        :param id: str
          Databricks service principal ID.
        :param active: bool (optional)
          If this user is active
        :param application_id: str (optional)
          UUID relating to the service principal
        :param display_name: str (optional)
          String that represents a concatenation of given and family names.
        :param entitlements: List[:class:`ComplexValue`] (optional)
          Entitlements assigned to the service principal. See [assigning entitlements] for a full list of
          supported values.

          [assigning entitlements]: https://docs.databricks.com/administration-guide/users-groups/index.html#assigning-entitlements
        :param external_id: str (optional)
        :param groups: List[:class:`ComplexValue`] (optional)
        :param roles: List[:class:`ComplexValue`] (optional)
          Corresponds to AWS instance profile/arn role.
        :param schemas: List[:class:`ServicePrincipalSchema`] (optional)
          The schema of the List response.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Databricks service principal ID.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                active:
                  type: string
                  description: If this user is active
                application_id:
                  type: string
                  description: UUID relating to the service principal
                display_name:
                  type: string
                  description: >-
                    String that represents a concatenation of given and family
                    names.
                entitlements:
                  type: string
                  description: >-
                    Entitlements assigned to the service principal. See
                    [assigning entitlements] for a full list of supported
                    values. [assigning entitlements]:
                    https://docs.databricks.com/administration-guide/users-groups/index.html#assigning-entitlements
                external_id:
                  type: string
                  description: ':param groups: List[:class:`ComplexValue`] (optional)'
                groups:
                  type: string
                roles:
                  type: string
                  description: Corresponds to AWS instance profile/arn role.
                schemas:
                  type: string
                  description: The schema of the List response.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: ServicePrincipalsV2API
  /api/2.0/preview/scim/v2/Users:
    post:
      operationId: users_v2_create
      summary: >-
        Creates a new user in the Databricks workspace. This new user will also
        be added to the Databricks
      tags:
        - iam
        - users_v2
      description: >-
        Creates a new user in the Databricks workspace. This new user will also
        be added to the Databricks

        account.


        :param active: bool (optional)
          If this user is active
        :param display_name: str (optional)
          String that represents a concatenation of given and family names. For example `John Smith`. This
          field cannot be updated through the Workspace SCIM APIs when [identity federation is enabled]. Use
          Account SCIM APIs to update `displayName`.

          [identity federation is enabled]: https://docs.databricks.com/administration-guide/users-groups/best-practices.html#enable-identity-federation
        :param emails: List[:class:`ComplexValue`] (optional)
          All the emails associated with the Databricks user.
        :param entitlements: List[:class:`ComplexValue`] (optional)
          Entitlements assigned to the user. See [assigning entitlements] for a full list of supported values.

          [assigning entitlements]: https://docs.databricks.com/administration-guide/users-groups/index.html#assigning-entitlements
        :param external_id: str (optional)
          External ID is not currently supported. It is reserved for future use.
        :param groups: List[:class:`ComplexValue`] (optional)

        :param id: str (optional)
          Databricks user ID.
        :param name: :class:`Name` (optional)

        :param roles: List[:class:`ComplexValue`] (optional)
          Corresponds to AWS instance profile/arn role.
        :param schemas: List[:class:`UserSchema`] (optional)
          The schema of the user.
        :param user_name: str (optional)
          Email address of the Databricks user.

        :returns: :class:`User`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                active:
                  type: string
                  description: If this user is active
                display_name:
                  type: string
                  description: >-
                    String that represents a concatenation of given and family
                    names. For example `John Smith`. This field cannot be
                    updated through the Workspace SCIM APIs when [identity
                    federation is enabled]. Use Account SCIM APIs to update
                    `displayName`. [identity federation is enabled]:
                    https://docs.databricks.com/administration-guide/users-groups/best-practices.html#enable-identity-federation
                emails:
                  type: string
                  description: All the emails associated with the Databricks user.
                entitlements:
                  type: string
                  description: >-
                    Entitlements assigned to the user. See [assigning
                    entitlements] for a full list of supported values.
                    [assigning entitlements]:
                    https://docs.databricks.com/administration-guide/users-groups/index.html#assigning-entitlements
                external_id:
                  type: string
                  description: >-
                    External ID is not currently supported. It is reserved for
                    future use.
                groups:
                  type: string
                  description: ':param id: str (optional) Databricks user ID.'
                id:
                  type: string
                name:
                  type: string
                  description: >-
                    :param roles: List[:class:`ComplexValue`] (optional)
                    Corresponds to AWS instance profile/arn role.
                roles:
                  type: string
                schemas:
                  type: string
                  description: The schema of the user.
                user_name:
                  type: string
                  description: Email address of the Databricks user.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: UsersV2API
    get:
      operationId: users_v2_list
      summary: Gets details for all the users associated with a Databricks workspace.
      tags:
        - iam
        - users_v2
      description: |-
        Gets details for all the users associated with a Databricks workspace.

        :param attributes: str (optional)
          Comma-separated list of attributes to return in response.
        :param count: int (optional)
          Desired number of results per page.
        :param excluded_attributes: str (optional)
          Comma-separated list of attributes to exclude in response.
        :param filter: str (optional)
          Query by which the results have to be filtered. Supported operators are equals(`eq`),
          contains(`co`), starts with(`sw`) and not equals(`ne`). Additionally, simple expressions can be
          formed using logical operators - `and` and `or`. The [SCIM RFC] has more details but we currently
          only support simple expressions.

          [SCIM RFC]: https://tools.ietf.org/html/rfc7644#section-3.4.2.2
        :param sort_by: str (optional)
          Attribute to sort the results. Multi-part paths are supported. For example, `userName`,
          `name.givenName`, and `emails`.
        :param sort_order: :class:`ListSortOrder` (optional)
          The order to sort the results.
        :param start_index: int (optional)
          Specifies the index of the first result. First item is number 1.

        :returns: Iterator over :class:`User`
      parameters:
        - name: attributes
          in: query
          required: false
          schema:
            type: string
          description: Comma-separated list of attributes to return in response.
        - name: count
          in: query
          required: false
          schema:
            type: string
          description: Desired number of results per page.
        - name: excluded_attributes
          in: query
          required: false
          schema:
            type: string
          description: Comma-separated list of attributes to exclude in response.
        - name: filter
          in: query
          required: false
          schema:
            type: string
          description: >-
            Query by which the results have to be filtered. Supported operators
            are equals(`eq`), contains(`co`), starts with(`sw`) and not
            equals(`ne`). Additionally, simple expressions can be formed using
            logical operators - `and` and `or`. The [SCIM RFC] has more details
            but we currently only support simple expressions. [SCIM RFC]:
            https://tools.ietf.org/html/rfc7644#section-3.4.2.2
        - name: sort_by
          in: query
          required: false
          schema:
            type: string
          description: >-
            Attribute to sort the results. Multi-part paths are supported. For
            example, `userName`, `name.givenName`, and `emails`.
        - name: sort_order
          in: query
          required: false
          schema:
            type: string
          description: The order to sort the results.
        - name: start_index
          in: query
          required: false
          schema:
            type: string
          description: Specifies the index of the first result. First item is number 1.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListUsersResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: UsersV2API
  /api/2.0/preview/scim/v2/Users/{id}:
    delete:
      operationId: users_v2_delete
      summary: >-
        Deletes a user. Deleting a user from a Databricks workspace also removes
        objects associated with the
      tags:
        - iam
        - users_v2
      description: >-
        Deletes a user. Deleting a user from a Databricks workspace also removes
        objects associated with the

        user.


        :param id: str
          Unique ID for a user in the Databricks workspace.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Unique ID for a user in the Databricks workspace.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: UsersV2API
    get:
      operationId: users_v2_get
      summary: Gets information for a specific user in Databricks workspace.
      tags:
        - iam
        - users_v2
      description: |-
        Gets information for a specific user in Databricks workspace.

        :param id: str
          Unique ID for a user in the Databricks workspace.
        :param attributes: str (optional)
          Comma-separated list of attributes to return in response.
        :param count: int (optional)
          Desired number of results per page.
        :param excluded_attributes: str (optional)
          Comma-separated list of attributes to exclude in response.
        :param filter: str (optional)
          Query by which the results have to be filtered. Supported operators are equals(`eq`),
          contains(`co`), starts with(`sw`) and not equals(`ne`). Additionally, simple expressions can be
          formed using logical operators - `and` and `or`. The [SCIM RFC] has more details but we currently
          only support simple expressions.

          [SCIM RFC]: https://tools.ietf.org/html/rfc7644#section-3.4.2.2
        :param sort_by: str (optional)
          Attribute to sort the results. Multi-part paths are supported. For example, `userName`,
          `name.givenName`, and `emails`.
        :param sort_order: :class:`GetSortOrder` (optional)
          The order to sort the results.
        :param start_index: int (optional)
          Specifies the index of the first result. First item is number 1.

        :returns: :class:`User`
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Unique ID for a user in the Databricks workspace.
        - name: attributes
          in: query
          required: false
          schema:
            type: string
          description: Comma-separated list of attributes to return in response.
        - name: count
          in: query
          required: false
          schema:
            type: string
          description: Desired number of results per page.
        - name: excluded_attributes
          in: query
          required: false
          schema:
            type: string
          description: Comma-separated list of attributes to exclude in response.
        - name: filter
          in: query
          required: false
          schema:
            type: string
          description: >-
            Query by which the results have to be filtered. Supported operators
            are equals(`eq`), contains(`co`), starts with(`sw`) and not
            equals(`ne`). Additionally, simple expressions can be formed using
            logical operators - `and` and `or`. The [SCIM RFC] has more details
            but we currently only support simple expressions. [SCIM RFC]:
            https://tools.ietf.org/html/rfc7644#section-3.4.2.2
        - name: sort_by
          in: query
          required: false
          schema:
            type: string
          description: >-
            Attribute to sort the results. Multi-part paths are supported. For
            example, `userName`, `name.givenName`, and `emails`.
        - name: sort_order
          in: query
          required: false
          schema:
            type: string
          description: The order to sort the results.
        - name: start_index
          in: query
          required: false
          schema:
            type: string
          description: Specifies the index of the first result. First item is number 1.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: UsersV2API
    patch:
      operationId: users_v2_patch
      summary: >-
        Partially updates a user resource by applying the supplied operations on
        specific user attributes.
      tags:
        - iam
        - users_v2
      description: >-
        Partially updates a user resource by applying the supplied operations on
        specific user attributes.


        :param id: str
          Unique ID in the Databricks workspace.
        :param operations: List[:class:`Patch`] (optional)

        :param schemas: List[:class:`PatchSchema`] (optional)
          The schema of the patch request. Must be ["urn:ietf:params:scim:api:messages:2.0:PatchOp"].
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Unique ID in the Databricks workspace.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                operations:
                  type: string
                  description: >-
                    :param schemas: List[:class:`PatchSchema`] (optional) The
                    schema of the patch request. Must be
                    ["urn:ietf:params:scim:api:messages:2.0:PatchOp"].
                schemas:
                  type: string
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: UsersV2API
    put:
      operationId: users_v2_update
      summary: Replaces a user's information with the data supplied in request.
      tags:
        - iam
        - users_v2
      description: |-
        Replaces a user's information with the data supplied in request.

        :param id: str
          Databricks user ID.
        :param active: bool (optional)
          If this user is active
        :param display_name: str (optional)
          String that represents a concatenation of given and family names. For example `John Smith`. This
          field cannot be updated through the Workspace SCIM APIs when [identity federation is enabled]. Use
          Account SCIM APIs to update `displayName`.

          [identity federation is enabled]: https://docs.databricks.com/administration-guide/users-groups/best-practices.html#enable-identity-federation
        :param emails: List[:class:`ComplexValue`] (optional)
          All the emails associated with the Databricks user.
        :param entitlements: List[:class:`ComplexValue`] (optional)
          Entitlements assigned to the user. See [assigning entitlements] for a full list of supported values.

          [assigning entitlements]: https://docs.databricks.com/administration-guide/users-groups/index.html#assigning-entitlements
        :param external_id: str (optional)
          External ID is not currently supported. It is reserved for future use.
        :param groups: List[:class:`ComplexValue`] (optional)
        :param name: :class:`Name` (optional)
        :param roles: List[:class:`ComplexValue`] (optional)
          Corresponds to AWS instance profile/arn role.
        :param schemas: List[:class:`UserSchema`] (optional)
          The schema of the user.
        :param user_name: str (optional)
          Email address of the Databricks user.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Databricks user ID.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                active:
                  type: string
                  description: If this user is active
                display_name:
                  type: string
                  description: >-
                    String that represents a concatenation of given and family
                    names. For example `John Smith`. This field cannot be
                    updated through the Workspace SCIM APIs when [identity
                    federation is enabled]. Use Account SCIM APIs to update
                    `displayName`. [identity federation is enabled]:
                    https://docs.databricks.com/administration-guide/users-groups/best-practices.html#enable-identity-federation
                emails:
                  type: string
                  description: All the emails associated with the Databricks user.
                entitlements:
                  type: string
                  description: >-
                    Entitlements assigned to the user. See [assigning
                    entitlements] for a full list of supported values.
                    [assigning entitlements]:
                    https://docs.databricks.com/administration-guide/users-groups/index.html#assigning-entitlements
                external_id:
                  type: string
                  description: >-
                    External ID is not currently supported. It is reserved for
                    future use.
                groups:
                  type: string
                  description: ':param name: :class:`Name` (optional)'
                name:
                  type: string
                roles:
                  type: string
                  description: Corresponds to AWS instance profile/arn role.
                schemas:
                  type: string
                  description: The schema of the user.
                user_name:
                  type: string
                  description: Email address of the Databricks user.
      responses:
        '200':
          description: Success
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: UsersV2API
  /api/2.0/permissions/authorization/passwords/permissionLevels:
    get:
      operationId: users_v2_get_permission_levels
      summary: Gets the permission levels that a user can have on an object.
      tags:
        - iam
        - users_v2
      description: |-
        Gets the permission levels that a user can have on an object.


        :returns: :class:`GetPasswordPermissionLevelsResponse`
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetPasswordPermissionLevelsResponse'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: UsersV2API
  /api/2.0/permissions/authorization/passwords:
    get:
      operationId: users_v2_get_permissions
      summary: >-
        Gets the permissions of all passwords. Passwords can inherit permissions
        from their root object.
      tags:
        - iam
        - users_v2
      description: >-
        Gets the permissions of all passwords. Passwords can inherit permissions
        from their root object.



        :returns: :class:`PasswordPermissions`
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PasswordPermissions'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: UsersV2API
    put:
      operationId: users_v2_set_permissions
      summary: >-
        Sets permissions on an object, replacing existing permissions if they
        exist. Deletes all direct
      tags:
        - iam
        - users_v2
      description: >-
        Sets permissions on an object, replacing existing permissions if they
        exist. Deletes all direct

        permissions if none are specified. Objects can inherit permissions from
        their root object.


        :param access_control_list: List[:class:`PasswordAccessControlRequest`]
        (optional)


        :returns: :class:`PasswordPermissions`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                access_control_list:
                  type: string
                  description: ':returns: :class:`PasswordPermissions`'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PasswordPermissions'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: UsersV2API
    patch:
      operationId: users_v2_update_permissions
      summary: >-
        Updates the permissions on all passwords. Passwords can inherit
        permissions from their root object.
      tags:
        - iam
        - users_v2
      description: >-
        Updates the permissions on all passwords. Passwords can inherit
        permissions from their root object.


        :param access_control_list: List[:class:`PasswordAccessControlRequest`]
        (optional)


        :returns: :class:`PasswordPermissions`
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                access_control_list:
                  type: string
                  description: ':returns: :class:`PasswordPermissions`'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PasswordPermissions'
        default:
          description: Error response
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  message:
                    type: string
      x-stackql-sdk-source: UsersV2API
components:
  schemas:
    AccessControlRequest:
      type: object
      properties:
        group_name:
          type: string
        permission_level:
          $ref: '#/components/schemas/PermissionLevel'
        service_principal_name:
          type: string
          description: application ID of a service principal
        user_name:
          type: string
          description: name of the user
    AccessControlResponse:
      type: object
      properties:
        all_permissions:
          type: array
          items:
            $ref: '#/components/schemas/Permission'
        display_name:
          type: string
          description: Display name of the user or service principal.
        group_name:
          type: string
          description: name of the group
        service_principal_name:
          type: string
          description: Name of the service principal.
        user_name:
          type: string
          description: name of the user
    AccountGroup:
      type: object
      properties:
        account_id:
          type: string
        displayName:
          type: string
          description: String that represents a human-readable group name
        externalId:
          type: string
          description: external_id should be unique for identifying groups
        id:
          type: string
          description: Databricks group ID
        members:
          type: array
          items:
            $ref: '#/components/schemas/ComplexValue'
        meta:
          $ref: '#/components/schemas/ResourceMeta'
          description: Container for the group identifier. Workspace local versus account.
        roles:
          type: array
          items:
            $ref: '#/components/schemas/ComplexValue'
          description: Indicates if the group has the admin role.
    AccountServicePrincipal:
      type: object
      properties:
        account_id:
          type: string
        active:
          type: boolean
          description: If this user is active
        applicationId:
          type: string
          description: UUID relating to the service principal
        displayName:
          type: string
          description: String that represents a concatenation of given and family names.
        externalId:
          type: string
        id:
          type: string
          description: Databricks service principal ID.
        roles:
          type: array
          items:
            $ref: '#/components/schemas/ComplexValue'
          description: Indicates if the group has the admin role.
    AccountUser:
      type: object
      properties:
        account_id:
          type: string
        active:
          type: boolean
          description: If this user is active
        displayName:
          type: string
          description: >-
            String that represents a concatenation of given and family names.
            For example `John Smith`.
        emails:
          type: array
          items:
            $ref: '#/components/schemas/ComplexValue'
          description: All the emails associated with the Databricks user.
        externalId:
          type: string
          description: >-
            External ID is not currently supported. It is reserved for future
            use.
        id:
          type: string
          description: Databricks user ID.
        name:
          $ref: '#/components/schemas/Name'
        roles:
          type: array
          items:
            $ref: '#/components/schemas/ComplexValue'
          description: Indicates if the group has the admin role.
        userName:
          type: string
          description: Email address of the Databricks user.
    Actor:
      type: object
      properties:
        actor_id:
          type: integer
      description: >-
        represents an identity trying to access a resource - user or a service
        principal group can be a
            principal of a permission set assignment but an actor is always a user or a service principal
    CheckPolicyResponse:
      type: object
      properties:
        consistency_token:
          $ref: '#/components/schemas/ConsistencyToken'
        is_permitted:
          type: boolean
      required:
        - consistency_token
    ComplexValue:
      type: object
      properties:
        display:
          type: string
        primary:
          type: boolean
        $ref:
          type: string
        type:
          type: string
        value:
          type: string
    ConsistencyToken:
      type: object
      properties:
        value:
          type: string
      required:
        - value
    DeleteWorkspacePermissionAssignmentResponse:
      type: object
      properties: {}
    GetAssignableRolesForResourceResponse:
      type: object
      properties:
        roles:
          type: array
          items:
            $ref: '#/components/schemas/Role'
    GetPasswordPermissionLevelsResponse:
      type: object
      properties:
        permission_levels:
          type: array
          items:
            $ref: '#/components/schemas/PasswordPermissionsDescription'
    GetPermissionLevelsResponse:
      type: object
      properties:
        permission_levels:
          type: array
          items:
            $ref: '#/components/schemas/PermissionsDescription'
    GrantRule:
      type: object
      properties:
        role:
          type: string
        principals:
          type: array
          items:
            type: string
          description: >-
            Principals this grant rule applies to. A principal can be a user
            (for end users), a service principal (for applications and compute
            workloads), or an account group. Each principal has its own
            identifier format: * users/<USERNAME> * groups/<GROUP_NAME> *
            servicePrincipals/<SERVICE_PRINCIPAL_APPLICATION_ID>
      required:
        - role
    Group:
      type: object
      properties:
        displayName:
          type: string
        entitlements:
          type: array
          items:
            $ref: '#/components/schemas/ComplexValue'
          description: >-
            Entitlements assigned to the group. See [assigning entitlements] for
            a full list of supported values. [assigning entitlements]:
            https://docs.databricks.com/administration-guide/users-groups/index.html#assigning-entitlements
        externalId:
          type: string
          description: external_id should be unique for identifying groups
        groups:
          type: array
          items:
            $ref: '#/components/schemas/ComplexValue'
        id:
          type: string
          description: Databricks group ID
        members:
          type: array
          items:
            $ref: '#/components/schemas/ComplexValue'
        meta:
          $ref: '#/components/schemas/ResourceMeta'
          description: Container for the group identifier. Workspace local versus account.
        roles:
          type: array
          items:
            $ref: '#/components/schemas/ComplexValue'
          description: Corresponds to AWS instance profile/arn role.
        schemas:
          type: array
          items:
            $ref: '#/components/schemas/GroupSchema'
          description: The schema of the group.
    ListAccountGroupsResponse:
      type: object
      properties:
        itemsPerPage:
          type: integer
        Resources:
          type: array
          items:
            $ref: '#/components/schemas/AccountGroup'
          description: User objects returned in the response.
        startIndex:
          type: integer
          description: >-
            Starting index of all the results that matched the request filters.
            First item is number 1.
        totalResults:
          type: integer
          description: Total results that match the request filters.
    ListAccountServicePrincipalsResponse:
      type: object
      properties:
        itemsPerPage:
          type: integer
        Resources:
          type: array
          items:
            $ref: '#/components/schemas/AccountServicePrincipal'
          description: User objects returned in the response.
        startIndex:
          type: integer
          description: >-
            Starting index of all the results that matched the request filters.
            First item is number 1.
        totalResults:
          type: integer
          description: Total results that match the request filters.
    ListAccountUsersResponse:
      type: object
      properties:
        itemsPerPage:
          type: integer
        Resources:
          type: array
          items:
            $ref: '#/components/schemas/AccountUser'
          description: User objects returned in the response.
        startIndex:
          type: integer
          description: >-
            Starting index of all the results that matched the request filters.
            First item is number 1.
        totalResults:
          type: integer
          description: Total results that match the request filters.
    ListGroupsResponse:
      type: object
      properties:
        itemsPerPage:
          type: integer
        Resources:
          type: array
          items:
            $ref: '#/components/schemas/Group'
          description: User objects returned in the response.
        schemas:
          type: array
          items:
            $ref: '#/components/schemas/ListResponseSchema'
          description: The schema of the service principal.
        startIndex:
          type: integer
          description: >-
            Starting index of all the results that matched the request filters.
            First item is number 1.
        totalResults:
          type: integer
          description: Total results that match the request filters.
    ListServicePrincipalResponse:
      type: object
      properties:
        itemsPerPage:
          type: integer
        Resources:
          type: array
          items:
            $ref: '#/components/schemas/ServicePrincipal'
          description: User objects returned in the response.
        schemas:
          type: array
          items:
            $ref: '#/components/schemas/ListResponseSchema'
          description: The schema of the List response.
        startIndex:
          type: integer
          description: >-
            Starting index of all the results that matched the request filters.
            First item is number 1.
        totalResults:
          type: integer
          description: Total results that match the request filters.
    ListUsersResponse:
      type: object
      properties:
        itemsPerPage:
          type: integer
        Resources:
          type: array
          items:
            $ref: '#/components/schemas/User'
          description: User objects returned in the response.
        schemas:
          type: array
          items:
            $ref: '#/components/schemas/ListResponseSchema'
          description: The schema of the List response.
        startIndex:
          type: integer
          description: >-
            Starting index of all the results that matched the request filters.
            First item is number 1.
        totalResults:
          type: integer
          description: Total results that match the request filters.
    MigratePermissionsResponse:
      type: object
      properties:
        permissions_migrated:
          type: integer
    Name:
      type: object
      properties:
        familyName:
          type: string
        givenName:
          type: string
          description: Given name of the Databricks user.
    ObjectPermissions:
      type: object
      properties:
        access_control_list:
          type: array
          items:
            $ref: '#/components/schemas/AccessControlResponse'
        object_id:
          type: string
        object_type:
          type: string
    PasswordAccessControlRequest:
      type: object
      properties:
        group_name:
          type: string
        permission_level:
          $ref: '#/components/schemas/PasswordPermissionLevel'
          description: Permission level
        service_principal_name:
          type: string
          description: application ID of a service principal
        user_name:
          type: string
          description: name of the user
    PasswordAccessControlResponse:
      type: object
      properties:
        all_permissions:
          type: array
          items:
            $ref: '#/components/schemas/PasswordPermission'
        display_name:
          type: string
          description: Display name of the user or service principal.
        group_name:
          type: string
          description: name of the group
        service_principal_name:
          type: string
          description: Name of the service principal.
        user_name:
          type: string
          description: name of the user
    PasswordPermission:
      type: object
      properties:
        inherited:
          type: boolean
        inherited_from_object:
          type: array
          items:
            type: string
        permission_level:
          $ref: '#/components/schemas/PasswordPermissionLevel'
          description: Permission level
    PasswordPermissions:
      type: object
      properties:
        access_control_list:
          type: array
          items:
            $ref: '#/components/schemas/PasswordAccessControlResponse'
        object_id:
          type: string
        object_type:
          type: string
    PasswordPermissionsDescription:
      type: object
      properties:
        description:
          type: string
        permission_level:
          $ref: '#/components/schemas/PasswordPermissionLevel'
          description: Permission level
    Patch:
      type: object
      properties:
        op:
          $ref: '#/components/schemas/PatchOp'
        path:
          type: string
          description: Selection of patch operation
        value:
          type: object
          description: Value to modify
    Permission:
      type: object
      properties:
        inherited:
          type: boolean
        inherited_from_object:
          type: array
          items:
            type: string
        permission_level:
          $ref: '#/components/schemas/PermissionLevel'
    PermissionAssignment:
      type: object
      properties:
        error:
          type: string
          description: >-
            Error response associated with a workspace permission assignment, if
            any.
        permissions:
          type: array
          items:
            $ref: '#/components/schemas/WorkspacePermission'
          description: The permissions level of the principal.
        principal:
          $ref: '#/components/schemas/PrincipalOutput'
          description: Information about the principal assigned to the workspace.
      description: >-
        The output format for existing workspace PermissionAssignment records,
        which contains some info
            for user consumption.
    PermissionAssignments:
      type: object
      properties:
        permission_assignments:
          type: array
          items:
            $ref: '#/components/schemas/PermissionAssignment'
    PermissionOutput:
      type: object
      properties:
        description:
          type: string
        permission_level:
          $ref: '#/components/schemas/WorkspacePermission'
    PermissionsDescription:
      type: object
      properties:
        description:
          type: string
        permission_level:
          $ref: '#/components/schemas/PermissionLevel'
    PrincipalOutput:
      type: object
      properties:
        display_name:
          type: string
          description: The display name of the principal.
        group_name:
          type: string
          description: >-
            The group name of the group. Present only if the principal is a
            group.
        principal_id:
          type: integer
          description: The unique, opaque id of the principal.
        service_principal_name:
          type: string
          description: >-
            The name of the service principal. Present only if the principal is
            a service principal.
        user_name:
          type: string
          description: The username of the user. Present only if the principal is a user.
      description: Information about the principal assigned to the workspace.
    ResourceInfo:
      type: object
      properties:
        id:
          type: string
        legacy_acl_path:
          type: string
          description: The legacy acl path of the current resource.
        parent_resource_info:
          $ref: '#/components/schemas/ResourceInfo'
          description: >-
            Parent resource info for the current resource. The parent may have
            another parent.
      required:
        - id
    ResourceMeta:
      type: object
      properties:
        resourceType:
          type: string
    Role:
      type: object
      properties:
        name:
          type: string
      required:
        - name
    RuleSetResponse:
      type: object
      properties:
        name:
          type: string
        etag:
          type: string
          description: >-
            Identifies the version of the rule set returned. Etag used for
            versioning. The response is at least as fresh as the eTag provided.
            Etag is used for optimistic concurrency control as a way to help
            prevent simultaneous updates of a rule set from overwriting each
            other. It is strongly suggested that systems make use of the etag in
            the read -> modify -> write pattern to perform rule set updates in
            order to avoid race conditions that is get an etag from a GET rule
            set request, and pass it with the PUT update request to identify the
            rule set version you are updating.
        grant_rules:
          type: array
          items:
            $ref: '#/components/schemas/GrantRule'
      required:
        - name
        - etag
    RuleSetUpdateRequest:
      type: object
      properties:
        name:
          type: string
        etag:
          type: string
          description: >-
            Identifies the version of the rule set returned. Etag used for
            versioning. The response is at least as fresh as the eTag provided.
            Etag is used for optimistic concurrency control as a way to help
            prevent simultaneous updates of a rule set from overwriting each
            other. It is strongly suggested that systems make use of the etag in
            the read -> modify -> write pattern to perform rule set updates in
            order to avoid race conditions that is get an etag from a GET rule
            set request, and pass it with the PUT update request to identify the
            rule set version you are updating.
        grant_rules:
          type: array
          items:
            $ref: '#/components/schemas/GrantRule'
      required:
        - name
        - etag
    ServicePrincipal:
      type: object
      properties:
        active:
          type: boolean
        applicationId:
          type: string
          description: UUID relating to the service principal
        displayName:
          type: string
          description: String that represents a concatenation of given and family names.
        entitlements:
          type: array
          items:
            $ref: '#/components/schemas/ComplexValue'
          description: >-
            Entitlements assigned to the service principal. See [assigning
            entitlements] for a full list of supported values. [assigning
            entitlements]:
            https://docs.databricks.com/administration-guide/users-groups/index.html#assigning-entitlements
        externalId:
          type: string
        groups:
          type: array
          items:
            $ref: '#/components/schemas/ComplexValue'
        id:
          type: string
          description: Databricks service principal ID.
        roles:
          type: array
          items:
            $ref: '#/components/schemas/ComplexValue'
          description: Corresponds to AWS instance profile/arn role.
        schemas:
          type: array
          items:
            $ref: '#/components/schemas/ServicePrincipalSchema'
          description: The schema of the List response.
    User:
      type: object
      properties:
        active:
          type: boolean
        displayName:
          type: string
          description: >-
            String that represents a concatenation of given and family names.
            For example `John Smith`. This field cannot be updated through the
            Workspace SCIM APIs when [identity federation is enabled]. Use
            Account SCIM APIs to update `displayName`. [identity federation is
            enabled]:
            https://docs.databricks.com/administration-guide/users-groups/best-practices.html#enable-identity-federation
        emails:
          type: array
          items:
            $ref: '#/components/schemas/ComplexValue'
          description: All the emails associated with the Databricks user.
        entitlements:
          type: array
          items:
            $ref: '#/components/schemas/ComplexValue'
          description: >-
            Entitlements assigned to the user. See [assigning entitlements] for
            a full list of supported values. [assigning entitlements]:
            https://docs.databricks.com/administration-guide/users-groups/index.html#assigning-entitlements
        externalId:
          type: string
          description: >-
            External ID is not currently supported. It is reserved for future
            use.
        groups:
          type: array
          items:
            $ref: '#/components/schemas/ComplexValue'
        id:
          type: string
          description: Databricks user ID.
        name:
          $ref: '#/components/schemas/Name'
        roles:
          type: array
          items:
            $ref: '#/components/schemas/ComplexValue'
          description: Corresponds to AWS instance profile/arn role.
        schemas:
          type: array
          items:
            $ref: '#/components/schemas/UserSchema'
          description: The schema of the user.
        userName:
          type: string
          description: Email address of the Databricks user.
    WorkspacePermissions:
      type: object
      properties:
        permissions:
          type: array
          items:
            $ref: '#/components/schemas/PermissionOutput'
    GetSortOrder:
      type: string
      enum:
        - ascending
        - descending
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    GroupSchema:
      type: string
      enum:
        - urn:ietf:params:scim:schemas:core:2.0:Group
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    ListResponseSchema:
      type: string
      enum:
        - urn:ietf:params:scim:api:messages:2.0:ListResponse
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    ListSortOrder:
      type: string
      enum:
        - ascending
        - descending
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    PasswordPermissionLevel:
      type: string
      enum:
        - CAN_USE
      description: Permission level
    PatchOp:
      type: string
      enum:
        - add
        - remove
        - replace
      description: Type of patch operation.
    PatchSchema:
      type: string
      enum:
        - urn:ietf:params:scim:api:messages:2.0:PatchOp
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    PermissionLevel:
      type: string
      enum:
        - CAN_ATTACH_TO
        - CAN_BIND
        - CAN_CREATE
        - CAN_EDIT
        - CAN_EDIT_METADATA
        - CAN_MANAGE
        - CAN_MANAGE_PRODUCTION_VERSIONS
        - CAN_MANAGE_RUN
        - CAN_MANAGE_STAGING_VERSIONS
        - CAN_MONITOR
        - CAN_MONITOR_ONLY
        - CAN_QUERY
        - CAN_READ
        - CAN_RESTART
        - CAN_RUN
        - CAN_USE
        - CAN_VIEW
        - CAN_VIEW_METADATA
        - IS_OWNER
      description: Permission level
    RequestAuthzIdentity:
      type: string
      enum:
        - REQUEST_AUTHZ_IDENTITY_SERVICE_IDENTITY
        - REQUEST_AUTHZ_IDENTITY_USER_CONTEXT
      description: >-
        Defines the identity to be used for authZ of the request on the server
        side. See one pager for

        for more information: http://go/acl/service-identity
    ServicePrincipalSchema:
      type: string
      enum:
        - urn:ietf:params:scim:schemas:core:2.0:ServicePrincipal
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    UserSchema:
      type: string
      enum:
        - urn:ietf:params:scim:schemas:core:2.0:User
        - urn:ietf:params:scim:schemas:extension:workspace:2.0:User
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
    WorkspacePermission:
      type: string
      enum:
        - ADMIN
        - UNKNOWN
        - USER
      description: |-
        Create a collection of name/value pairs.

        Example enumeration:

        >>> class Color(Enum):
        ...     RED = 1
        ...     BLUE = 2
        ...     GREEN = 3

        Access them by:

        - attribute access:

          >>> Color.RED
          <Color.RED: 1>

        - value lookup:

          >>> Color(1)
          <Color.RED: 1>

        - name lookup:

          >>> Color['RED']
          <Color.RED: 1>

        Enumerations can be iterated over, and know how many members they have:

        >>> len(Color)
        3

        >>> list(Color)
        [<Color.RED: 1>, <Color.BLUE: 2>, <Color.GREEN: 3>]

        Methods can be added to enumerations, and members can have their own
        attributes -- see the documentation for details.
  x-stackQL-resources:
    access_control:
      id: databricks_workspace.iam.access_control
      name: access_control
      title: Access Control
      methods:
        check:
          operation:
            $ref: '#/paths/~1api~12.0~1access-control~1check-policy-v2/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/access_control/methods/check'
        insert: []
        update: []
        delete: []
        replace: []
    assignable_roles:
      id: databricks_workspace.iam.assignable_roles
      name: assignable_roles
      title: Assignable Roles
      methods:
        get:
          operation:
            $ref: >-
              #/paths/~1api~12.0~1preview~1accounts~1access-control~1assignable-roles/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/assignable_roles/methods/get'
        insert: []
        update: []
        delete: []
        replace: []
    rule_sets:
      id: databricks_workspace.iam.rule_sets
      name: rule_sets
      title: Rule Sets
      methods:
        get:
          operation:
            $ref: >-
              #/paths/~1api~12.0~1preview~1accounts~1access-control~1rule-sets/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          operation:
            $ref: >-
              #/paths/~1api~12.0~1preview~1accounts~1access-control~1rule-sets/put
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/rule_sets/methods/get'
        insert: []
        update: []
        delete: []
        replace:
          - $ref: '#/components/x-stackQL-resources/rule_sets/methods/update'
    current_user:
      id: databricks_workspace.iam.current_user
      name: current_user
      title: Current User
      methods:
        get:
          operation:
            $ref: '#/paths/~1api~12.0~1preview~1scim~1v2~1Me/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/current_user/methods/get'
        insert: []
        update: []
        delete: []
        replace: []
    groups_v2:
      id: databricks_workspace.iam.groups_v2
      name: groups_v2
      title: Groups V2
      methods:
        create:
          operation:
            $ref: '#/paths/~1api~12.0~1preview~1scim~1v2~1Groups/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1api~12.0~1preview~1scim~1v2~1Groups/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.Resources
        delete:
          operation:
            $ref: '#/paths/~1api~12.0~1preview~1scim~1v2~1Groups~1{id}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1api~12.0~1preview~1scim~1v2~1Groups~1{id}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        patch:
          operation:
            $ref: '#/paths/~1api~12.0~1preview~1scim~1v2~1Groups~1{id}/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        replace:
          operation:
            $ref: '#/paths/~1api~12.0~1preview~1scim~1v2~1Groups~1{id}/put'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/groups_v2/methods/get'
          - $ref: '#/components/x-stackQL-resources/groups_v2/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/groups_v2/methods/create'
        update:
          - $ref: '#/components/x-stackQL-resources/groups_v2/methods/patch'
        delete:
          - $ref: '#/components/x-stackQL-resources/groups_v2/methods/delete'
        replace:
          - $ref: '#/components/x-stackQL-resources/groups_v2/methods/replace'
    permission_migration:
      id: databricks_workspace.iam.permission_migration
      name: permission_migration
      title: Permission Migration
      methods:
        migrate:
          operation:
            $ref: '#/paths/~1api~12.0~1permissionmigration/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select: []
        insert:
          - $ref: >-
              #/components/x-stackQL-resources/permission_migration/methods/migrate
        update: []
        delete: []
        replace: []
    permissions:
      id: databricks_workspace.iam.permissions
      name: permissions
      title: Permissions
      methods:
        get:
          operation:
            $ref: >-
              #/paths/~1api~12.0~1permissions~1{request_object_type}~1{request_object_id}/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        set:
          operation:
            $ref: >-
              #/paths/~1api~12.0~1permissions~1{request_object_type}~1{request_object_id}/put
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          operation:
            $ref: >-
              #/paths/~1api~12.0~1permissions~1{request_object_type}~1{request_object_id}/patch
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/permissions/methods/get'
        insert: []
        update:
          - $ref: '#/components/x-stackQL-resources/permissions/methods/update'
        delete: []
        replace:
          - $ref: '#/components/x-stackQL-resources/permissions/methods/set'
    permission_levels:
      id: databricks_workspace.iam.permission_levels
      name: permission_levels
      title: Permission Levels
      methods:
        get:
          operation:
            $ref: >-
              #/paths/~1api~12.0~1permissions~1{request_object_type}~1{request_object_id}~1permissionLevels/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/permission_levels/methods/get'
        insert: []
        update: []
        delete: []
        replace: []
    service_principals_v2:
      id: databricks_workspace.iam.service_principals_v2
      name: service_principals_v2
      title: Service Principals V2
      methods:
        create:
          operation:
            $ref: '#/paths/~1api~12.0~1preview~1scim~1v2~1ServicePrincipals/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1api~12.0~1preview~1scim~1v2~1ServicePrincipals/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.Resources
        delete:
          operation:
            $ref: >-
              #/paths/~1api~12.0~1preview~1scim~1v2~1ServicePrincipals~1{id}/delete
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1api~12.0~1preview~1scim~1v2~1ServicePrincipals~1{id}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        patch:
          operation:
            $ref: >-
              #/paths/~1api~12.0~1preview~1scim~1v2~1ServicePrincipals~1{id}/patch
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        replace:
          operation:
            $ref: '#/paths/~1api~12.0~1preview~1scim~1v2~1ServicePrincipals~1{id}/put'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/service_principals_v2/methods/get'
          - $ref: >-
              #/components/x-stackQL-resources/service_principals_v2/methods/list
        insert:
          - $ref: >-
              #/components/x-stackQL-resources/service_principals_v2/methods/create
        update:
          - $ref: >-
              #/components/x-stackQL-resources/service_principals_v2/methods/patch
        delete:
          - $ref: >-
              #/components/x-stackQL-resources/service_principals_v2/methods/delete
        replace:
          - $ref: >-
              #/components/x-stackQL-resources/service_principals_v2/methods/replace
    users_v2:
      id: databricks_workspace.iam.users_v2
      name: users_v2
      title: Users V2
      methods:
        create:
          operation:
            $ref: '#/paths/~1api~12.0~1preview~1scim~1v2~1Users/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        list:
          operation:
            $ref: '#/paths/~1api~12.0~1preview~1scim~1v2~1Users/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.Resources
        delete:
          operation:
            $ref: '#/paths/~1api~12.0~1preview~1scim~1v2~1Users~1{id}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        get:
          operation:
            $ref: '#/paths/~1api~12.0~1preview~1scim~1v2~1Users~1{id}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        patch:
          operation:
            $ref: '#/paths/~1api~12.0~1preview~1scim~1v2~1Users~1{id}/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        replace:
          operation:
            $ref: '#/paths/~1api~12.0~1preview~1scim~1v2~1Users~1{id}/put'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/users_v2/methods/get'
          - $ref: '#/components/x-stackQL-resources/users_v2/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/users_v2/methods/create'
        update:
          - $ref: '#/components/x-stackQL-resources/users_v2/methods/patch'
        delete:
          - $ref: '#/components/x-stackQL-resources/users_v2/methods/delete'
        replace:
          - $ref: '#/components/x-stackQL-resources/users_v2/methods/replace'
    password_permission_levels:
      id: databricks_workspace.iam.password_permission_levels
      name: password_permission_levels
      title: Password Permission Levels
      methods:
        get:
          operation:
            $ref: >-
              #/paths/~1api~12.0~1permissions~1authorization~1passwords~1permissionLevels/get
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: >-
              #/components/x-stackQL-resources/password_permission_levels/methods/get
        insert: []
        update: []
        delete: []
        replace: []
    password_permissions:
      id: databricks_workspace.iam.password_permissions
      name: password_permissions
      title: Password Permissions
      methods:
        get:
          operation:
            $ref: '#/paths/~1api~12.0~1permissions~1authorization~1passwords/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        set:
          operation:
            $ref: '#/paths/~1api~12.0~1permissions~1authorization~1passwords/put'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
        update:
          operation:
            $ref: '#/paths/~1api~12.0~1permissions~1authorization~1passwords/patch'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/password_permissions/methods/get'
        insert: []
        update:
          - $ref: >-
              #/components/x-stackQL-resources/password_permissions/methods/update
        delete: []
        replace:
          - $ref: '#/components/x-stackQL-resources/password_permissions/methods/set'
